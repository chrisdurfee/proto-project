-- Initial database setup for Proto Project
-- This script runs automatically when the MariaDB container starts for the first time

-- Create additional databases if needed
-- CREATE DATABASE IF NOT EXISTS proto_test;

-- Grant additional permissions if needed
-- GRANT ALL PRIVILEGES ON proto_test.* TO 'proto_user'@'%';

-- Create some initial tables or run migrations here
-- Or leave empty if your app handles migrations

FLUSH PRIVILEGES;
