import { PageHeader as TablePageHeader } from "@components/pages/types/page-header.js";

/**
 * This will create a page header for the connection page.
 *
 * @returns {object}
 */
export const PageHeader = () => (
	TablePageHeader({ title: 'Connections' }, [

	])
);