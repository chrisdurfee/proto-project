import { Model } from "@base-framework/base";

/**
 * OrganizationModel
 *
 * This model is used to handle the organization model.
 *
 * @type {typeof Model}
 */
export const OrganizationModel = Model.extend({
	url: '/api/user/organization',

	xhr: {

	}
});