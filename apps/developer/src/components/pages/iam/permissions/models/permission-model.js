import { Model } from "@base-framework/base";

/**
 * PermissionModel
 *
 * This model is used to handle the permission model.
 *
 * @type {typeof Model}
 */
export const PermissionModel = Model.extend({
	url: '/api/user/permission',

	xhr: {

	}
});