import { Model } from "@base-framework/base";

/**
 * RoleModel
 *
 * This model is used to handle the role model.
 *
 * @type {typeof Model}
 */
export const RoleModel = Model.extend({
	url: '/api/user/role',

	xhr: {

	}
});