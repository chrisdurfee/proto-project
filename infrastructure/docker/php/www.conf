; Pool Configuration for PHP-FPM
; www pool - handles regular API and static-PHP requests
; OPTIMIZED FOR HIGH CONCURRENCY
;
; NOTE: Long-lived SSE streams (paths ending in /sync) are routed by Apache
; to the dedicated [sse] pool defined in sse.conf. Keeping streams off this
; pool is what allows request_terminate_timeout below to be tight.

[www]

; Unix user/group of processes
user = www-data
group = www-data

; The address on which to accept FastCGI requests
; For Docker, listen on TCP socket for easier debugging
; For production, consider unix socket: listen = /var/run/php-fpm.sock
listen = 127.0.0.1:9000

; Set listen(2) backlog - increased for high concurrency
listen.backlog = 65535

; Set permissions for unix socket (not applicable for TCP)
;listen.owner = www-data
;listen.group = www-data
;listen.mode = 0660

; List of addresses (IPv4/IPv6) of FastCGI clients which are allowed to connect
listen.allowed_clients = 127.0.0.1

; PROCESS MANAGER CONFIGURATION - HIGH CONCURRENCY
; Options: static, dynamic, ondemand
; For high traffic: use 'static' for predictable resource usage
; For variable traffic: use 'dynamic' for adaptive scaling
pm = dynamic

; Observed worker RSS is ~45-50MB each. The web container's memory limit is
; 2GB (infrastructure/docker-compose.production.yaml), so max_children is
; capped at 35 (35 * 50MB = ~1.75GB, leaving headroom for Apache + base
; container overhead). Was previously set to 100, which could reserve up to
; ~5GB under a real traffic spike -- well past the 2GB cgroup limit -- and
; risk an OOM-kill of the whole container. Raise this only alongside the
; container's memory limit.
pm.max_children = 35

; The number of child processes created on startup (only for dynamic pm)
pm.start_servers = 10

; The desired minimum number of idle server processes (only for dynamic pm)
pm.min_spare_servers = 8

; The desired maximum number of idle server processes (only for dynamic pm)
pm.max_spare_servers = 20

; The number of requests each child process should execute before respawning
; Helps prevent memory leaks - higher value for stable apps
pm.max_requests = 1000

; The URI to view the FPM status page
pm.status_path = /fpm-status

; The ping URI to call the monitoring page of FPM
ping.path = /fpm-ping

; HARD BACKSTOP for stuck workers.
;
; SSE streams have moved to the [sse] pool, so this timeout no longer needs to
; be 0 / 300s+. A tight bound here is what prevents a single hanging request
; from sitting on a worker forever — which previously caused PHP-FPM saturation
; when even /health.php started queuing.
;
; Greater than php max_execution_time so PHP's own timer fires first under
; normal conditions; this is the safety net for cases PHP can't recover from
; (deadlocked DB query, hung external HTTP call with no timeout, infinite loop).
;
; Raised to 600s (matches max_input_time) so uploading a large (up to 500M)
; file over a slow mobile connection has time to finish receiving the body
; before this backstop kills the worker.
request_terminate_timeout = 600s

; Dump a backtrace for any request still running after this long. Look in the
; container error log (`docker logs proto-web`) for these to find slow paths
; before they become outages.
request_slowlog_timeout = 30s

; The log file for slow requests
slowlog = /proc/self/fd/2

; Set open file descriptor rlimit - increased for high concurrency
rlimit_files = 65535

; Set max core size rlimit
rlimit_core = 0

; Chdir to this directory at the start
chdir = /var/www/html

; Redirect worker stdout and stderr into main error log
catch_workers_output = yes

; Decorate worker output with prefix and suffix
decorate_workers_output = no

; Clear environment in FPM workers
clear_env = no

; SECURITY: Limit extensions processed by FPM
security.limit_extensions = .php

; Pass environment variables (important for Docker)
env[HOSTNAME] = $HOSTNAME
env[PATH] = /usr/local/bin:/usr/bin:/bin
env[TMP] = /tmp
env[TMPDIR] = /tmp
env[TEMP] = /tmp

; PHP-FPM will pass these environment variables to PHP scripts
env[DB_HOST] = $DB_HOST
env[DB_PORT] = $DB_PORT
env[DB_DATABASE] = $DB_DATABASE
env[DB_USERNAME] = $DB_USERNAME
env[DB_PASSWORD] = $DB_PASSWORD
env[REDIS_HOST] = $REDIS_HOST
env[REDIS_PORT] = $REDIS_PORT
env[REDIS_PASSWORD] = $REDIS_PASSWORD
env[APP_ENV] = $APP_ENV

; Additional php.ini defines for this pool
php_admin_value[error_log] = /proc/self/fd/2
php_admin_flag[log_errors] = on

; Performance tuning.
; max_execution_time stays at 100s so PHP's own deadline fires before the
; FPM-level request_terminate_timeout (600s) under normal conditions.
; max_input_time is raised to 600s (matching request_terminate_timeout) so
; receiving a large (up to 500M) upload body over a slow connection doesn't
; get cut off before the request even starts executing.
php_value[max_execution_time] = 100
php_value[max_input_time] = 600
php_value[memory_limit] = 512M
php_value[post_max_size] = 550M
php_value[upload_max_filesize] = 500M
